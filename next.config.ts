import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["fast.wistia.com", "embed.wistia.com", "embed-ssl.wistia.com"],
  },
};

export default nextConfig;
